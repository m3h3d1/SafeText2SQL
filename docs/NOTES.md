# Notes

- Current generator is rule-based and exists only to exercise the security pipeline.
- Replace `text2sql.py` with an actual LLM-backed generator in the next iteration.
- Current model probing is heuristic and intended only as a starter baseline.
